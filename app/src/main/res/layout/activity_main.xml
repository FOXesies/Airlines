<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".di.AirlineApplication">

    <FrameLayout
        android:id="@+id/nav_fragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@+id/bottom_navigation"
        android:background="@color/back" />

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_navigation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:labelVisibilityMode="labeled"
        android:paddingHorizontal="10dp"
        android:background="@color/back"
        style="@style/Widget.MaterialComponents.NavigationRailView.Colored.Compact"
        app:itemBackground="@android:color/transparent"
        app:itemIconTint="@drawable/state_item_bottom_bar"
        app:itemTextColor="@drawable/state_item_bottom_bar"
        android:layout_alignParentBottom="true"
        app:menu="@menu/main_bottom_bar"/>

</RelativeLayout>